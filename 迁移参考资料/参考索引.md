# 参考索引与阅读顺序

## 1. 先读本资料包

1. `README.md`：完整技术原理、实现细节、伪代码、风险和测试。
2. `接口与时序速查.md`：开发时快速查全局接口和关键时序。
3. `迁移清单.md`：在新项目中按阶段落地与验收。

## 2. 再按主题读取 Pastoral 现有源码（只读）

### 正文提取

- `../酒馆正文提取与处理指导文件 (1).txt`
  - 四种正文入口比较。
  - 模式 A/B/C/D 的取舍。
  - 选项应在显示正则前提取。
- `../js/extract.js`
  - 当前实际采用模式 A。
  - `extractOptions()`。
  - `extractCleanContent()`。
  - `normalizeUpdateVariable()`。
  - `patchToMvuCommands()`。

### MVU

- `../本项目mvu文件.txt`
  - 当前选定的 `schema.ts`。
  - 当前选定的 `initvar.yaml`。
- `../js/mvu.js`
  - 等待初始化。
  - 最新真实楼完整快照。
  - 完整写回、超时写回、确定性结算与事实锁定。
- `../_types_split/15-ejs-mvu.txt`
  - `MvuData` 类型。
  - `getMvuData/parseMessage/replaceMvuData` 官方类型说明。

### 第二 API 与预设

- `../js/settings.js`
  - 设置结构与规范化。
  - 当前源码与迁移资料均将 `blockDepthEntries` 默认设为 `false`；旧设置通过版本 2 一次性迁移。
- `../js/api.js`
  - `custom_api` 路由。
  - 提示词组装。
  - 重试与格式纠正。
  - none/current/fixed。
  - 固定预设 1 秒 + 2 秒事务。
- `../_types_split/06-generate.txt`
  - `generate/generateRaw`。
  - `custom_api/ordered_prompts/overrides/generation_id`。
- `../_types_split/09-preset.txt`
  - `getLoadedPresetName()` 与 `getPreset('in_use')` 的区别。
  - `loadPreset/replacePreset`。

### 主回复与伪零层

- `../js/chat.js`
  - `/send + /trigger await=true`。
  - 生成事件与楼层轮询。
  - 聚合所有真实消息。
- `../js/host.js`
  - 0 楼唯一宿主。
  - 非 0 楼自毁。
  - 父文档视觉隐藏与同源降级。
- `../_types_split/04-chat-message.txt`
  - 消息读写与真实 message ID。
- `../_types_split/14-events.txt`
  - 生成和消息事件。

## 3. 测试是当前行为的最终证据

- `../test/api.js`
  - JSON Patch 验证。
  - MVU 命令桥。
  - 第二 API 重试。
  - 固定预设延时、恢复和锁外网络等待。
  - 任务仅一份。
- `../test/settings.js`
  - 配置规范化与旧字段迁移。
- `../test/smoke.js`
  - 0 楼宿主、非 0 楼自毁、独立预览。
  - MVU 读取 lastMessageId。
  - 真实 message ID 编辑删除。
- `../test/iframe.js`
  - 同源父页面隐藏非 0 楼。
  - 保留 0 楼和原生输入区。
  - 当前 iframe 沉浸模式。
- `../test/rules.js`
  - 日常/归寝规则与输出格式。

## 4. 历史设计文档只用于理解演进

下列文档可能包含已经被后续实测替换的方案，阅读时必须回到当前源码与测试核验：

- `../docs/specs/2026-08-01-model-lifecycle-and-preset-design.md`
- `../docs/specs/2026-08-01-variable-request-assembly-design.md`
- `../docs/superpowers/specs/2026-08-01-cross-platform-variable-settlement-design.md`
- `../docs/superpowers/plans/2026-08-01-cross-platform-variable-settlement.md`
- `../plan.md`

典型历史差异：

- 内部空白预设已被 `generateRaw` none 模式替代。
- compile/inject 选择已退出生产路径。
- 固定模式不再依赖 `preset_name`，而是真实短切主预设。
- 当前父页面接管保留原生输入区和 0 楼名字条。
- 迁移推荐的深度注入屏蔽默认是未勾选；这与当前功能源码默认值不同。

## 5. 资料包边界

本文件夹只包含迁移说明，不包含或覆盖任何 Pastoral 功能源码。迁移时应复制所需思想和最小代码骨架到新项目，不应把这里的 Markdown 当可直接运行的构建产物。
